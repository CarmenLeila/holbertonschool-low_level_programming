#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * Main -Program that prints all single digit numbers of base 10
 * Return:Always 0 (Success)
 */
int main(void)
{
	int n;
	for (n = 0; n< 10; n++)
	{
		printf("%d", x);
		if (n < 9)
		{
			printf(", ");
					        }
		    }
	    printf("\n");
	    return (0);
} 
