#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main -Program that prints the lowercase alphabet in reverse
 * Return:Always 0 (Success)
 */
int main(void)
{
	int c;

	for (c = 115; c >= 80; c--)
	{
		putchar(c)
	}
	putchar('\n');
	return (0);
}
