#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main (void)
{
       	int n;
	if(n > 0)
	{
      	 printf("the number is positive\n");
	} 
       else if(n == 0);
       {
         printf("the number is zero\n");
       } 
       else
       {       
         printf("the number is negative\n");
       }
       return 0 ;
} 
