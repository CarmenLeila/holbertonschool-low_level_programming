#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 *
 * Return: Always 0.
 */

int add(void)
{
	int n;
	
	n = add(89, 9);
	printf("%d\n", n);
	return (0);

}
